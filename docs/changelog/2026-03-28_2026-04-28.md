# Изменения за период 28.03 – 28.04.2026

**Коммитов:** 63  
**Файлов изменено:** 260  
**Строк добавлено:** +20 501  
**Строк удалено:** −30 619  

---

## 1. Роли и доступ (v1.6.07)

- Настроены права Рабочего и Мастера через middleware `WorkerOnly` / `MasterOnly`
- Добавлены тесты авторизации: `MasterAccessTest`, `MasterDepartmentAccessTest`

## 2. Мобильная адаптация (v1.6.09 – v1.7.3)

Полный responsive-слой для четырёх разделов:

- Журнал приёмок (`logs.blade.php`)
- Страницы stone-receptions (index, show, create, edit)
- Раздел raw-batches (index + все операции)
- Дашборд пильщика + навигация мастера

## 3. Цветовая дифференциация (v1.6.17 – v1.6.18)

- Карточки приёмок и партий сырья окрашиваются по типу камня через `Product::getColorBySku()`
- Цветовая подсветка в дашборде, остатки в пикере приёмки

## 4. Контрагенты и поступления сырья (v1.7.1 – v1.7.3)

- Новая модель `Counterparty` + синхронизация с МойСклад
- Модуль `SupplierOrder` (поступления сырья): модель, контроллер, сервис, 5 views, Form Request'ы, тесты
- Страница просмотра поступления (v2.0.7)
- Синхронизация остатков МойСклад после создания приёмки

## 5. МойСклад — синхронизация техопераций (v1.7.5, v1.8.0 – v2.1.6)

- `DocumentNaming` — единая генерация имён документов + обработка коллизий
- Полная синхронизация техопераций по партиям и приёмкам (v1.9.0)
- Кастомное имя + дата `moment` в техоперации (v2.0.1)
- Фильтрация нулевых позиций при создании техоперации (v1.9.6)
- Синхронизация при передаче/корректировке партий (v2.0.5)
- Корректный расчёт `processingSum` с зарплатой пильщика (v1.9.3)
- История приёмок в описании техоперации (v2.1.6)
- Рефакторинг МойСклад-сервисов в `app/Services/Moysklad/`

## 6. Настройки системы из БД (v1.9.5, v1.9.9)

- Таблица `settings`, модель `Setting`, сидеры
- Базовая ставка, расходы, статусы МойСклад — конфигурируются из UI
- Дашборд для мастера расширен ключами настроек

## 7. Затраты производства (v2.0.0 – v2.0.2)

- Поле `worker_cost_per_m2` в позициях приёмки (`stone_reception_items`)
- `processing_sum` в партиях сырья (зарплата пильщика + расходы)
- Ставка мастера: поле `is_small_tile` + `master_cost_per_m2`, дашборд мастера с итогом (v2.0.8)
- Приватность выплат на дашборде + кнопка-глазок (v1.8.0)

## 8. Частичная передача и возврат партий (v2.0.2, v2.0.5)

- Частичная передача/возврат партий сырья
- Поле `notes` для партий
- Валидация статусов при передаче
- Передача из статуса `new`

## 9. Множественные должности (v2.0.9)

- Поле `position` (строка) → `positions[]` (массив) в модели `Worker`
- Миграция + фиксы навигации и интеграции с МойСклад

## 10. Рефакторинг сервисного слоя (v2.1.0 – v2.1.5)

- Вынос бизнес-логики из контроллеров в сервисы:
  `WorkerDashboardService`, `WorkerService`, `ProductService`, `StoneReceptionService`, `RawMaterialBatchService`
- Мобильные карточки вынесены в партиалы: `raw-batch-card`, `reception-card`, `reception-log-card`
- Унификация `product-picker` и `filters` через `resources/views/partials/`
- Рефакторинг блока складов отделов в настройках (v2.1.5)

## 11. Отделы (v2.1.2)

- Модель `Department`, `DepartmentController`, миграции, сидер `DepartmentsTableSeeder`
- Views: `admin/departments/create`, `admin/departments/show`
- Склады по умолчанию для отделов (`store_defaults`)

---

## Тестовое покрытие

Добавлены тест-файлы (~25 новых):

| Группа | Файлы |
|---|---|
| AdminSettings | `AdminSettingControllerTest`, `ProductPieceRateTest`, `SettingModelTest` |
| Auth | `LoginTest`, `MasterAccessTest` |
| Counterparty | `CounterpartyTest` |
| MasterDashboard | `MasterDashboardControllerTest` |
| Moysklad | `MoyskladBaseServiceTest`, `MoyskladMoveServiceTest`, `MoyskladPurchaseOrderServiceTest`, `MoyskladServiceTest`, `MoyskladSupplyServiceTest`, `StockSyncServiceTest`, `StoneReceptionSyncServiceTest` |
| RawBatch | `BatchNumberTest`, `BatchStatusTest`, `RawBatchNewStatusTest`, `RawBatchPagesTest`, `RawBatchStoreTest`, `RawBatchTransferTest`, `RawMaterialBatchServiceTest` |
| StoneReception | `ReceptionActionsTest`, `ReceptionCopyAndApiTest`, `ReceptionStatusCascadeTest`, `StoneReceptionServiceTest` |
| Worker | `MasterDepartmentAccessTest`, `WorkerControllerTest`, `WorkerDashboardTest`, `WorkerServiceTest` |
| Прочее | `StoreControllerTest`, `SupplierOrderTest`, `ProductTest` |

---

## Итог

За месяц приложение выросло от базовой системы приёмок до полноценной производственной системы:
добавлен модуль поступлений сырья с контрагентами, глубокая интеграция МойСклад (техоперации, остатки, документы),
расчёт затрат производства, настройки системы из БД, полный мобильный UI,
и проведён рефакторинг архитектуры под двухслойный паттерн контроллер → сервис.
